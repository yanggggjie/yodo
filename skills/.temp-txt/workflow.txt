先走跑脚本。`task/` 里有对得上的就直接跑并报告；没有一份直接对应、但现有脚本能拼，就在 `tmp/` 组合，试通了 `mv` 进 `task/` 再跑并报告。跑脚本做不成、又拼不出来，才走录抓包：`record-start` 等人在新窗口做一遍，`record-stop` 交出 `recordDir`。有了抓包再走写重放：对着 `timeline.jsonl` 和 request 在 `tmp/` 用 `yodo.run` + `pageForOrigin` + `evaluate` 写脚本，试通了交给跑脚本那一套留下并报告（检验 URL 由 agent 看 task 脚本现拼，脚本不 return `url`）。需要浏览器的命令自己连 Chrome；连不上就把 stdout 的 `guide` 原样念给用户，停，等「好了」再重跑刚才那条命令——不单独成一项，写在每条会连浏览器的 use case 里。
