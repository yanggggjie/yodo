{
  "name": "yodo",
  "version": "3.0.0",
  "type": "module",
  "description": "Use the user's logged-in Chrome to record a site, then run scripts",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "npx -y -p typescript@^5.8.3 tsc -p tsconfig.json",
    "test": "find . -name '*.selfcheck.ts' | sort | while read -r f; do node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@ghostery/adblocker": "^2.18.2",
    "tldts": "^7.4.11",
    "write-file-atomic": "^7.0.1",
    "@types/node": "^22.16.5",
    "@types/write-file-atomic": "^4.0.3"
  }
}
